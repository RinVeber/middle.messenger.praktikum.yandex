import './index.scss'

const template = () => `
            <div class="writer">
            {{{buttonFileAdd}}}
                {{{inputMessage}}}
                {{{buttonSend}}}
            </div>
`;

export default template;
